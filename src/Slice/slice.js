import { createSlice } from "@reduxjs/toolkit";

const initialState = {
  
};

export const atcSlice = createSlice({
  name: "Add To Cart",
  initialState,
  reducers: {
    
  },
});

export const { } = atcSlice.actions;

export default atcSlice.reducer;
